import { SunflowerObservation } from './sunflower-observation';

describe('SunflowerObservation', () => {
  it('should create an instance', () => {
    expect(new SunflowerObservation()).toBeTruthy();
  });
});
