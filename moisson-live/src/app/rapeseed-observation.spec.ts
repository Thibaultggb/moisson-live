import { RapeseedObservation } from './rapeseed-observation';

describe('RapeseedObservation', () => {
  it('should create an instance', () => {
    expect(new RapeseedObservation()).toBeTruthy();
  });
});
